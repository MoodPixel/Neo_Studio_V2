"""Built-in Image Style Stack extension package.

Phase B creates the self-contained extension boundary only. Runtime registration,
CSV routes, prompt merging, output metadata, and replay are implemented in later
migration phases.
"""

EXTENSION_ID = "style_stack"
EXTENSION_ROOT = "neo_extensions/built_in/image.style_stack"
